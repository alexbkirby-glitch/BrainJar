# Lodestone — brain-jar
> 2149 seeds · 118 stacks · 62 critical · 2026-07-05

## How to use

1. **Before writing code** — check if the relevant stack SKILL file has a matching seed
2. **Error in context** — find the symptom below, fetch that stack's SKILL file
3. **MCP available** — call `lookup_symptom("paste error here")` for full search
4. **Critical patterns** — always read `lodestone-critical.md` for the active stack

Seed format: **WRONG** (antipattern) → **CORRECT** (fix) → **Symptom** (observable failure).
When applying a seed, note: "Applying `{seed_id}` — {reason}."

## Critical seeds (62 total — read lodestone-critical.md)

- ⚠ **[angular]** `renderer2_not_nativeelement` — application working in the browser but crashing in Angular Universal SSR because
- ⚠ **[ansible]** `become_privilege_escalation` — sudo inside shell commands failing in environments that require interactive pass
- ⚠ **[assembly]** `calling_convention_callee_saved` — intermittent data corruption or crashes after calling external functions.
- ⚠ **[assembly]** `memory_barriers_ordering` — lock-free data structures with rare but irreproducible corruption under load.
- ⚠ **[bash]** `bash_set_e_u` — script continues after a failing command, producing incorrect results silently.
- ⚠ **[celery]** `task_serializer_json` — Celery accepting and deserialising a malicious pickle payload from a compromised
- ⚠ **[control]** `pid_integral_windup_saturation_overshoot` — auto-scaler adds far too many instances immediately after a scaling limit is rem
- ⚠ **[control]** `dead_time_loop_delay_phase_margin_erosion` — controller is tuned with short test loops but fails with longer production loops
- ⚠ **[control]** `negative_feedback_sign_error_positive_runaway` — system diverges immediately on any perturbation; corrective action makes the pro
- ⚠ **[django]** `form_cleaned_data` — type errors or XSS vulnerabilities from using raw POST data instead of cleaned f
- ⚠ **[economics]** `vickrey_auction_truthful_dominant_strategy` — a first-price auction platform assuming truthful bids for value estimation; a se
- ⚠ **[express]** `query_param_validation` — database errors from unexpected string values where integers are expected, or 50
- ⚠ **[flask]** `jinja2_xss_autoescape` — XSS vulnerability from user-supplied content rendered with | safe without server
- ⚠ **[gdextension]** `gdext_gdextension_json` — extension does not load, no error shown, classes unavailable in editor.
- ⚠ **[gdscript]** `match_exhaustive` — code after match executes incorrectly because the expected branch was never ente
- ⚠ **[grpc]** `backwards_compatible_changes` — deserialization silently returning zero values for all fields after a proto fiel
- ⚠ **[htmx]** `csrf_protection` — HTMX form submission returning 403 Forbidden from CSRF middleware that wasn't up
- ⚠ **[json]** `number_precision_large_integers` — database IDs returned from a server are subtly wrong on the client, e.g., last t
- ⚠ **[laravel]** `sanctum_api_tokens` — full OAuth server setup for an internal SPA that Sanctum's cookie-based auth han
- ⚠ **[llm-integration]** `llm_output_validation` — SQL injection via LLM-generated queries, or code execution exploits.
- _(+ 42 more in lodestone-critical.md)_

## Symptom index (high blast — see SKILL files for detail)

- `ab_peeking` [ab-testing] — Experiment stopped at p = 0.04 after week 1; rolled out; effect disappears; was …
- `ab_no_primary_metric` [ab-testing] — Experiment 'wins' on signup rate but loses on revenue; team picks signup as the …
- `ab_novelty_effect` [ab-testing] — New UI ships to 50% of users, immediate 15% engagement lift; rolled out; engagem…
- `ab_sample_ratio_mismatch` [ab-testing] — Experiment shows significant lift; post-hoc SRM check reveals 42/58 split; resul…
- `ab_holdout_contamination` [ab-testing] — Notification feature tested by user; treated users notify control users; control…
- `ab_guardrail_metrics_ignored` [ab-testing] — Feature reduces bounce rate but increases page load time 20%; shipped without ch…
- `ab_long_term_vs_short_term` [ab-testing] — More aggressive push notifications increase short-term opens but reduce 30-day r…
- `activity_lifecycle_awareness` [android] — crash or leak from a callback firing after Activity is destroyed because onPause…
- `viewmodel_no_context` [android] — memory leak detected by LeakCanary: Activity retained by ViewModel after rotatio…
- `room_main_thread` [android] — 'Cannot access database on the main thread' IllegalStateException, or ANR from b…
- `datastore_over_shared_prefs` [android] — ANR from SharedPreferences.commit() on the main thread, or concurrent write corr…
- `network_security_config` [android] — SSL certificate validation disabled in production, making the app vulnerable to …
- `room_database_migration` [android] — user data wiped on app update because a migration was missing and destructive mi…
- `async_pipe_not_subscribe` [angular] — memory leak from subscriptions not cleaned up when navigating away from a compon…
- `route_resolvers` [angular] — content popping in after navigation complete, causing layout shifts and flash of…
- `xor_zeroing_register` [assembly] — unnecessary instruction bytes and stale dependency chains in tight loops.
- `tail_call_jmp` [assembly] — stack overflow from deeply recursive functions that should use O(1) stack space.
- `bash_temp_files` [bash] — temp files collide between concurrent script instances, or temp files persist af…
- `epistasis_feature_interaction_cannot_test_in_isolation` [bridge] — unit tests pass for feature A, unit tests pass for feature B, but enabling both …
- `c_buffer_overflow_strcpy` [c] — Stack corruption, segfault, or silent memory overwrite; attackers can inject she…
- `c_use_after_free` [c] — Undefined behavior, data corruption, or security vulnerability (type confusion, …
- `c_format_string_vulnerability` [c] — Attacker reads stack memory via %x/%s specifiers or writes arbitrary memory via …
- `c_double_free` [c] — Heap corruption, crash, or security vulnerability; malloc metadata is corrupted …
- `c_volatile_missing_mmio` [c] — Polling loop never exits because compiler optimizes the memory read away; hardwa…
- `c_stack_array_overflow` [c] — Crash on function entry, stack overflow into adjacent stack frame, or silent cor…

## Stacks: machine-learning(⚠6) · web-security(⚠6) · mathematics(⚠5) · real-estate(⚠4) · control(⚠3) · universal(⚠3) · rails(⚠3) · lodestone(⚠2) · mysql(⚠2) · assembly(⚠2) · music(⚠1) · economics(⚠1) · seq-model-internals(⚠1) · express(⚠1) · zig(⚠1) · laravel(⚠1) · sqlite(⚠1) · angular(⚠1) · django(⚠1) · flask(⚠1)

Files: `lodestone-nano.md` (this) · `lodestone-critical.md` · `{stack}-SKILL.md` per stack